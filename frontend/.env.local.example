# AIMakerLab Frontend 환경 변수 설정

# ================================
# Django 백엔드 API 설정
# ================================

# ⚠️ 보안 주의:
# - NEXT_PUBLIC_* 변수는 클라이언트에 노출됩니다!
# - 프로덕션에서는 API_BASE_URL (서버 전용)을 사용하세요
# - 프록시를 통해 실제 백엔드 URL을 숨깁니다

# Django API 서버 URL (클라이언트에 노출됨 - 개발용)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Django API 서버 URL (서버 전용 - 클라이언트에 노출 안 됨)
# 프로덕션에서는 이 변수를 사용하고, 클라이언트는 프록시(/api/backend)를 통해 접근
API_BASE_URL=http://localhost:8000

# ================================
# 데이터 소스 모드 설정
# ================================

# 전역 데이터 소스 모드 (json 또는 api)
# json: public 폴더의 JSON 파일 사용
# api: Django 백엔드 API 사용
NEXT_PUBLIC_DATA_SOURCE=json

# ================================
# Debug 모드 설정 (페이지별)
# ================================
# true: JSON 파일 사용 (개발/테스트)
# false: Django API 사용 (프로덕션)

# Accounts 페이지
NEXT_PUBLIC_DEBUG_ACCOUNTS_PROFILE=true
NEXT_PUBLIC_DEBUG_ACCOUNTS_COURSES=true

# Inquiry 페이지
NEXT_PUBLIC_DEBUG_INQUIRY_INQUIRIES=true
NEXT_PUBLIC_DEBUG_INQUIRY_SCHEDULES=true
NEXT_PUBLIC_DEBUG_INQUIRY_OUTREACH=true

# Products 페이지
NEXT_PUBLIC_DEBUG_PRODUCTS_PRODUCTS=true
NEXT_PUBLIC_DEBUG_PRODUCTS_VIDEOS=true
NEXT_PUBLIC_DEBUG_PRODUCTS_QUOTE_ITEMS=true
NEXT_PUBLIC_DEBUG_PRODUCTS_QUOTE_INQUIRIES=true
NEXT_PUBLIC_DEBUG_PRODUCTS_REVIEWS=true

# Gallery 페이지
NEXT_PUBLIC_DEBUG_GALLERY_WORKS=true
NEXT_PUBLIC_DEBUG_GALLERY_REVIEWS=true

# Curriculum 페이지
NEXT_PUBLIC_DEBUG_CURRICULUM_CURRICULUMS=true

# Home 페이지
NEXT_PUBLIC_DEBUG_HOME_CONTENT=true

# ================================
# 배포 환경 설정 (프로덕션)
# ================================

# Docker Compose 환경
# BACKEND_URL=http://backend:8000

# Nginx + Gunicorn 환경
# BACKEND_URL=http://127.0.0.1:8000

# Kubernetes 환경
# BACKEND_URL=http://backend-service:8000

# API 모드로 전환
# NEXT_PUBLIC_DATA_SOURCE=api
# NEXT_PUBLIC_USE_API_ACCOUNTS=true
# NEXT_PUBLIC_USE_API_INQUIRY=true
# NEXT_PUBLIC_USE_API_PRODUCTS=true
# NEXT_PUBLIC_USE_API_GALLERY=true
# NEXT_PUBLIC_USE_API_CURRICULUM=true
# NEXT_PUBLIC_USE_API_HOME=true
